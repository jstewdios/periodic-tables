{
  "name": "periodic-tables",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check dist/app.js && node --check dist/elements.js && node --check dist/chemistry.js && node --check dist/audio.js",
    "dev": "python3 -m http.server 4173 --directory dist"
  }
}
